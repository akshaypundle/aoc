#include <algorithm>
#include <cmath>
#include <iostream>
#include "library.h"
#include <map>
#include <set>
#include <sstream>
#include <stack>
#include <string>
#include <utility>
#include <vector>

using namespace std;
using namespace library;

class Problem {
  public:
    void readInput() {
      string line;
      getline(cin, line);

      while(!cin.eof()) {
        getline(cin, line);
      }
    }

    long solve() {
      return 0;
    }

};

int main() {
  Problem p;
  p.readInput();
  cout << p.solve() << "\n";
}
